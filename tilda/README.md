# Tilda-код сайта vezminarin.cz

Эти файлы не собираются и не деплоятся откуда-то — они физически вставлены
как HTML/CSS/JS-блоки в редакторе Tilda. Здесь их копия только для того,
чтобы было где посмотреть, что реально на сайте, без захода в Tilda.

**Важно:** это не источник правды в реальном времени. Если кто-то поправит
код прямо в Tilda и забудет обновить копию здесь — файлы разойдутся. При
любом следующем изменении конкретного блока: сначала возьмите текущую
версию отсюда, отредактируйте, при обновлении на сайте — обновите и файл
здесь тем же движением.

## pages/ — код целых страниц

| Файл | Где на сайте | Что делает |
|---|---|---|
| `catalog-filter.html` | `/page118819546.html` (временно = главная страница, каталог) | Вкладки категорий, фильтры по цветам/цвету/высоте, промо-вставки других категорий в ленте "Vše" |
| `occasions.html` | `/members/occasions` | Даты и получатели: карточки людей, привязка праздников, импорт .ics, автопредложение сохранить получателя из истории заказов, автозаполнение адреса через Google Places |
| `b2b-cabinet.html` | `/members/b2b` | Личный кабинет для клиентов с привязанной фирмой: баланс, счета, история заказов. Синяя тема — сознательное визуальное отделение от обычного личного кабинета |
| `orders.html` | `/members/orders` | История заказов: статус-степпер (Potvrzeno/Na cestě/Doručeno), детали заказа, стикеры, баннер на коллекцию. Читает `window.__memberData.orders`, сама держит свой собственный опрос `member-data` (раньше был продублирован дважды подряд в коде страницы — оставлен один экземпляр) |

## blocks/ — блоки внутри других страниц

| Файл | Где физически вставлен | Что делает |
|---|---|---|
| `member-header-widget.html` | Верх `/members/` (главный экран личного кабинета) | Плитка "ближайший повод" (свои даты приоритетнее общего календаря) + плитка депозита |
| `b2b-tile.html` | `/members/` под остальными плитками | Плитка "NARIN Business" — ведёт на `/members/b2b`, если у клиента есть фирма, иначе на `/b2b` |
| `loyalty-card.html` | `/members/` | Карта лояльности: уровни Sapphire/Silver/Gold/Platinum, штрихкод, прогресс до следующего уровня |
| `profile-header.html` | `/members/` (самый верх) | Аватар/имя/выпадающее меню профиля, плитки заказов/подписки/B2B. **Также сам содержит свою копию опроса `member-data`** (обнаружено 2026-09-04 — раньше в репозитории этого не было, кто-то вставил код прямо в Tilda в обход этого файла). Дублирует то же самое, что делает `member-data-bootstrap.html` — оба независимо пишут в `window.__memberData`. Если оба реально стоят на странице одновременно — это избыточно (двойной поллинг), но не ломается; надёжнее оставить только один |
| `checkout-recipient-picker.html` | Zero Block-шаг "Příjemce" на странице оплаты, `#rec1935176163` | Чипы с сохранёнными получателями — подставляет имя/телефон в реальные поля заказа. **Абсолютные координаты на холсте настраиваются вручную в редакторе Tilda**, при переносе блока код не трогать, только X/Y/ширину/высоту |
| `catalog-custom-badges.html` | Страница каталога, рядом с `catalog-filter.html` | Плашка в углу карточки товара: свой текст от менеджера (`badge_text`/`badge_color` из `product_stickers`), или автоматически "Zbývá N ks" при остатке < 3. При `quantity === 0` плашку **не показывает** — это специально: сообщением "нет сегодня" занимается `catalog-availability-badges.html`, дублировать резкой красной "Vyprodáno" не нужно |
| `catalog-availability-badges.html` | Страница каталога, рядом с `catalog-filter.html` | "Doručíme dnes" / "Doručíme zítra" / "Doručíme <дата>" под названием товара — по `get_available_products()` (для "ohapka" синхронизируется триггером с `quantity`, для остальных — ручной тумблер в кабинете менеджера) и `get_special_order_products()` |
| `checkout-points-promo.html` | Страница оплаты (`/payment`) | Поле промокода на кассе — и обычные скидочные коды, и списание баллов (кратно 100, до 1000). Показывает баланс (`.user-balance`) через `member-data` с токеном. Проверка суммы/лимита списания идёт через `tilda-webhook` (`action: get_promo`) — **эта проверка всё ещё принимает голый email, не токен** (см. предупреждение в README ниже) |
| `checkout-date-blocker.html` | Страница оплаты (`/payment`) | Блокирует в календаре доставки: разово закрытые дни склада (`shop_closed_dates`); сегодняшнюю дату — если в `tcart.products` есть товар без наличия сегодня (`get_available_products()`, тот же источник, что у `catalog-availability-badges.html`); диапазон дней до ближайшей возможной даты — если в корзине спецзаказ (`get_special_order_products()`, тот же расчёт 2 рабочих дней, что и в плашке "Doručíme <дата>"). При клике на заблокированный день показывает всплывающую подсказку с причиной |
| `lk-auth-gate.html` | Вставка кода на весь сайт (глобальный код проекта в Tilda, не привязан к одной странице) | Оверлей с одноразовым подтверждением email (код на почту), выдаёт `lk_auth_token`. Показывается один раз на 60 дней на любой странице сайта — все остальные виджеты просто читают уже готовый токен |
| `member-data-bootstrap.html` | `/members/` (личный кабинет, отдельно от `member-header-widget.html`) | Опрашивает `member-data` (баланс/tier/заказы/транзакции), кладёт результат в `window.__memberData`. Сам никакого UI подтверждения не показывает — полагается на `lk-auth-gate.html`, который уже отработал раньше на странице |

## Как это связано с supabase/

Все виджеты выше дёргают:
- `supabase/functions/personal-dates/` — даты, получатели, автопредложения из истории заказов
- `supabase/functions/member-data/` — баланс, tier, история заказов и транзакций для карты лояльности (требует `lk_auth_token`, email берётся только из токена, не из тела запроса)
- `supabase/functions/tilda-webhook/` — приём заказа из Tilda: сохраняет заказ, считает начисляемые баллы по кэшбеку текущего уровня клиента, списывает баллы по промокоду, шлёт уведомление в Telegram. Реальное зачисление баллов происходит не здесь, а триггером `handle_payment_status_change_v2` в БД, когда статус заказа меняется на "Оплачено" — webhook только записывает посчитанное значение в `tilda_orders.earned_points`
- `supabase/migrations/20260903000000_recipients.sql` — таблица `recipients` и связь с `personal_dates`

Авторизация клиента везде одна и та же: `lk_auth_token` в `localStorage`
(HMAC-токен на 60 дней, выдаётся через email+одноразовый код, функция
`auth-verify`). Токен общий на весь домен — где бы ни стоял виджет, он
работает после одного подтверждения.

**Важный нюанс безопасности**, учтённый в каждом из этих файлов: перед
использованием токена клиент сверяет email внутри токена с тем, кто
реально сейчас залогинен через `tilda_members_profile` (родной Tilda
Members). При несовпадении — токен стирается. Без этой проверки на общем
устройстве один человек мог увидеть данные (включая телефон и адрес)
другого, если раньше на этом же устройстве подтверждал код кто-то ещё.
Если добавляете новый виджет с таким же `getAuthToken()` — переносите и
эту проверку тоже, не только сам токен.

**Открытая дыра (не закрыта, найдена 2026-09-17):** `tilda-webhook`
(`action: get_promo`, вызывается из `checkout-points-promo.html`) всё
ещё принимает голый `email` в теле запроса и возвращает по нему баланс
баллов клиента (`"Nedostatek bodů. Máte: N"`) без проверки токена —
тот же класс уязвимости, что был в `member-data` до фикса. Пока не
трогали, потому что исправление требует менять сразу два места
синхронно (клиент — слать токен вместо email, `tilda-webhook` —
проверять токен вместо доверия email) и разбираться с остальными
проблемами `get_promo`/`tilda-webhook` (список — в памяти проекта
`project_narin_tilda_scripts_map`), а не чинить по одной.
