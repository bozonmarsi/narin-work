-- Номер счёта присваивается один раз, при первой генерации PDF (не при
-- создании черновика) — так, если черновик потом удалят/пересчитают,
-- номера не расходуются впустую и не остаётся дырок в нумерации.
ALTER TABLE company_invoices ADD COLUMN IF NOT EXISTS invoice_number text UNIQUE;
