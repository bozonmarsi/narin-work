-- Фото/скан чека, загруженный при добавлении расхода (см. invoice-ingest
-- mode="extract") — раньше некуда было сохранить саму картинку, только
-- то, что из неё распознал Claude. Ссылка остаётся в записи расхода
-- насовсем, для сверки в будущем, а не только на момент ввода.
ALTER TABLE business_expenses ADD COLUMN IF NOT EXISTS receipt_url text;
